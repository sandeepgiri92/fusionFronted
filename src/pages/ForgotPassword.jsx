import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {ArrowLeft,Mail,ShieldCheck,KeyRound} from "lucide-react";import {useForgotPasswordMutation,useResetPasswordMutation} from "@/features/auth/authApi";import logo from "@/assets/fusion logo.png";import Toast from "@/components/Toast";
export default function ForgotPassword(){const nav=useNavigate();const [step,setStep]=useState(1);const [email,setEmail]=useState('');const [otp,setOtp]=useState('');const [password,setPassword]=useState('');const [toast,setToast]=useState(null);const [forgot,{isLoading:sendLoading}]=useForgotPasswordMutation();const [reset,{isLoading:resetLoading}]=useResetPasswordMutation();const send=async e=>{e.preventDefault();try{const r=await forgot({email}).unwrap();if(r.devOtp)setToast(`Development OTP: ${r.devOtp}`);else setToast('OTP sent to your email');setStep(2)}catch(err){setToast(err?.data?.message||'Unable to send OTP')}};const submit=async e=>{e.preventDefault();try{await reset({email,otp,newPassword:password}).unwrap();setToast('Password reset. Redirecting to login…');setTimeout(()=>nav('/admin/login'),900)}catch(err){setToast(err?.data?.message||'Unable to reset password')}};return <div className="login-page"><section className="login-art"><div className="art-content"><img src={logo}/><h1>Secure access,<br/><span style={{color:'#86a7ff'}}>restored.</span></h1><p>Reset your Fusion admin password with a one-time verification code delivered to your registered email.</p><div className="feature-pills"><span>10 minute OTP</span><span>Argon2 password hashing</span><span>Secure session cookies</span></div></div></section><section className="login-card-wrap"><div className="login-card"><div className="login-brand"><img src={logo}/><b>FUSION</b></div><Link to="/admin/login" style={{display:'inline-flex',alignItems:'center',gap:6,fontSize:11,color:'#64748b',textDecoration:'none',marginBottom:20}}><ArrowLeft size={14}/> Back to login</Link>{step===1?<><h2>Forgot password?</h2><p className="lead">Enter your admin email and we’ll send a verification code.</p><form className="login-form" onSubmit={send}><div className="field"><label>Registered email</label><div className="password-wrap"><Mail size={16} style={{position:'absolute',left:11,top:11,color:'#94a3b8'}}/><input className="input" style={{paddingLeft:34}} type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com" required/></div></div><button className="btn btn-primary" style={{justifyContent:'center'}} disabled={sendLoading}>{sendLoading?'Sending code…':<>Send OTP <ArrowLeft size={15} style={{transform:'rotate(180deg)'}}/></>}</button></form></>:<><h2>Verify & reset</h2><p className="lead">Code sent to <b>{email}</b>. Enter it below.</p><form className="login-form" onSubmit={submit}><div className="field"><label>6-digit OTP</label><div className="password-wrap"><ShieldCheck size={16} style={{position:'absolute',left:11,top:11,color:'#94a3b8'}}/><input className="input" style={{paddingLeft:34,letterSpacing:'.25em'}} inputMode="numeric" maxLength={6} value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,''))} required/></div></div><div className="field"><label>New password</label><div className="password-wrap"><KeyRound size={16} style={{position:'absolute',left:11,top:11,color:'#94a3b8'}}/><input className="input" style={{paddingLeft:34}} type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" required/></div></div><button className="btn btn-primary" style={{justifyContent:'center'}} disabled={resetLoading}>{resetLoading?'Resetting…':'Reset password'}</button><button type="button" className="btn btn-secondary" onClick={()=>setStep(1)}>Use another email</button></form></>}<div className="login-footer">For your security, reset codes expire after 10 minutes.</div></div></section><Toast message={toast} onClose={()=>setToast(null)}/></div>}
