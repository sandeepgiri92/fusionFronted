import {useState} from "react";import {Plus,Edit3,Trash2,RefreshCw,CreditCard} from "lucide-react";import AppModal from "@/components/AppModal";import Toast from "@/components/Toast";import {useGetExpensesQuery,useCreateExpenseMutation,useUpdateExpenseMutation,useDeleteExpenseMutation,useAddExpensePaymentMutation,useDeleteExpensePaymentMutation} from "@/features/expense/expenseApi";
const money=n=>`₹ ${Number(n||0).toLocaleString("en-IN",{minimumFractionDigits:2,maximumFractionDigits:2})}`;const today=()=>new Date().toISOString().slice(0,10);
export default function ExpensePage({type,title}){const hasPaymentStatus=["smc","other"].includes(String(type).toLowerCase());const [page,setPage]=useState(1),[modal,setModal]=useState(null),[toast,setToast]=useState(null),[form,setForm]=useState({date:today(),amount:"",remarks:"",expenseType:type});const {data,isLoading,refetch}=useGetExpensesQuery({type,page});const [create,{isLoading:creating}]=useCreateExpenseMutation(),[update,{isLoading:updating}]=useUpdateExpenseMutation(),[remove]=useDeleteExpenseMutation(),[addPayment,{isLoading:adding}]=useAddExpensePaymentMutation(),[deletePayment]=useDeleteExpensePaymentMutation();const rows=data?.expenses||[];
 const openNew=()=>{setForm({date:today(),amount:"",remarks:"",expenseType:type});setModal({expense:null})};const submit=async e=>{e.preventDefault();try{if(modal.expense)await update({id:modal.expense._id,...form,amount:Number(form.amount)}).unwrap();else await create({expenseData:{...form,amount:Number(form.amount)}}).unwrap();setToast(modal.expense?"Expense updated":"Expense added");setModal(null)}catch(err){setToast(err?.data?.message||"Unable to save expense")}};const edit=e=>{setForm({date:new Date(e.date).toISOString().slice(0,10),amount:e.amount,remarks:e.remarks||"",expenseType:e.expenseType});setModal({expense:e})};const del=async id=>{if(!confirm("Delete this expense?"))return;try{await remove(id).unwrap();setToast("Expense deleted")}catch(e){setToast(e?.data?.message||"Unable to delete")}};const openPay=e=>setModal({payment:e,amount:"",date:today(),remarks:""});const savePay=async e=>{e.preventDefault();try{await addPayment({id:modal.payment._id,amount:Number(modal.amount),paymentDate:modal.date,remarks:modal.remarks}).unwrap();setToast("Payment added");setModal(null)}catch(err){setToast(err?.data?.message||"Unable to add payment")}};const delPay=async(exp,pid)=>{if(!confirm("Remove this payment?"))return;try{await deletePayment({id:exp._id,paymentId:pid}).unwrap();setToast("Payment removed")}catch(e){setToast(e?.data?.message||"Unable to remove payment")}};
 return <><div className="page-title"><div><div className="eyebrow">Workspace / Expenses</div><h1>{title}</h1><p>Track and maintain every {type.toUpperCase()} expense.</p></div><button className="btn btn-primary" onClick={openNew}><Plus size={16}/> Add expense</button></div><div className="panel table-panel"><div className="toolbar"><div><b style={{fontSize:12}}>{data?.pagination?.totalData||0}</b><span style={{fontSize:10,color:"#94a3b8"}}> records</span></div><button className="small-btn" style={{marginLeft:"auto"}} onClick={refetch}><RefreshCw size={15}/></button></div>{isLoading?<div className="empty">Loading expenses…</div>:<><div className="table-wrap"><table className="table"><thead><tr><th>Date</th><th>Type</th><th>Remarks</th><th>Total</th>{hasPaymentStatus&&<><th>Paid</th><th>Due</th><th>Status</th></>}<th>Actions</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r._id}><td>{new Date(r.date).toLocaleDateString("en-IN")}</td><td style={{textTransform:"uppercase",fontWeight:700}}>{r.expenseType}</td><td>{r.remarks||"—"}</td><td><b>{money(r.amount)}</b></td>{hasPaymentStatus&&<><td>{money(r.totalPaid)}</td><td><b>{money(r.remainingAmount)}</b></td><td><span className={`status ${r.status}`}>{r.status==="paid"?"Paid":r.status==="partial"?"Partial":"Unpaid"}</span></td></>}<td><div className="actions">{hasPaymentStatus&&<button className="small-btn" title="Add payment" onClick={()=>openPay(r)}><CreditCard size={14}/></button>}<button className="small-btn" onClick={()=>edit(r)}><Edit3 size={14}/></button><button className="small-btn" onClick={()=>del(r._id)}><Trash2 size={14}/></button></div></td></tr>):<tr><td colSpan={hasPaymentStatus?8:5}><div className="empty">No expenses recorded.</div></td></tr>}</tbody></table></div><div className="pagination"><span>Page {data?.pagination?.currentPage||1} of {data?.pagination?.totalPages||1}</span><button className="btn btn-secondary" disabled={!data?.pagination?.hasPreviousPage} onClick={()=>setPage(page-1)}>Previous</button><button className="btn btn-secondary" disabled={!data?.pagination?.hasNextPage} onClick={()=>setPage(page+1)}>Next</button></div></>}</div>
 {modal?.expense!==undefined&&<AppModal title={`${modal.expense?"Edit":"Add"} expense`} onClose={()=>setModal(null)}><form onSubmit={submit}><div className="form-grid"><div className="field"><label>Date</label><input type="date" className="input" value={form.date} onChange={e=>setForm({...form,date:e.target.value})} required/></div><div className="field"><label>Amount</label><input type="number" min="0" step="0.01" className="input" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} required/></div><div className="field full"><label>Remarks</label><textarea className="textarea" rows="4" value={form.remarks} onChange={e=>setForm({...form,remarks:e.target.value})} required/></div></div><div className="form-actions"><button type="button" className="btn btn-secondary" onClick={()=>setModal(null)}>Cancel</button><button className="btn btn-primary" disabled={creating||updating}>{creating||updating?"Saving…":"Save expense"}</button></div></form></AppModal>}
 {modal?.payment&&<AppModal title="Add payment" onClose={()=>setModal(null)}><form onSubmit={savePay}><div className="payment-summary"><div><span>Total</span><b>{money(modal.payment.amount)}</b></div><div><span>Already paid</span><b>{money(modal.payment.totalPaid)}</b></div><div><span>Remaining</span><b>{money(modal.payment.remainingAmount)}</b></div></div><div className="form-grid"><div className="field"><label>Payment amount</label><input type="number" min="0.01" max={modal.payment.remainingAmount} step="0.01" className="input" value={modal.amount} onChange={e=>setModal({...modal,amount:e.target.value})} required/></div><div className="field"><label>Payment date</label><input type="date" className="input" value={modal.date} onChange={e=>setModal({...modal,date:e.target.value})} required/></div><div className="field full"><label>Remarks</label><input className="input" value={modal.remarks} onChange={e=>setModal({...modal,remarks:e.target.value})} placeholder="Cash, UPI, etc."/></div></div>{modal.payment.payments?.length>0&&<div className="payment-history"><h4>Payment history</h4>{modal.payment.payments.map(p=><div key={p._id}><span>{new Date(p.paymentDate).toLocaleDateString("en-IN")}</span><b>{money(p.amount)}</b><small>{p.remarks||""}</small><button type="button" className="small-btn" onClick={()=>delPay(modal.payment,p._id)}><Trash2 size={13}/></button></div>)}</div>}<div className="form-actions"><button type="button" className="btn btn-secondary" onClick={()=>setModal(null)}>Close</button><button className="btn btn-primary" disabled={adding}>Add payment</button></div></form></AppModal>}
 <Toast message={toast} type={toast?.toLowerCase().includes("unable")||toast?.toLowerCase().includes("cannot")?"error":"success"} onClose={()=>setToast(null)}/></>}
